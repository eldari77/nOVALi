# Docker Image

No prebuilt image archive is bundled with this source-first handoff. The repository-level first-run wizard builds `novali-v7-standalone:local` from this package when Docker is available.

Do not add an archive unless its checksum and build provenance match this exact package revision.
