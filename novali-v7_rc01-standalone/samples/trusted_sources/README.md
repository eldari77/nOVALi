# Trusted Source Samples

Trusted-source credentials are intentionally not included. Configure provider credentials only in a private local environment file or secret store.

Trusted-source retrieval remains governed and validator-gated. Public samples may describe the shape of configuration, but they must not contain real keys or private endpoint tokens.

