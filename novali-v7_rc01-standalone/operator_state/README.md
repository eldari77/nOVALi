# Operator State

This directory is writable runtime state for local operators.

The public repository should not track generated ledgers, operation results, board decisions, private runtime state, or secrets from this directory.
