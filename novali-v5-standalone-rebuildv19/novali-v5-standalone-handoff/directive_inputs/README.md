# Directive Inputs

Place operator-authored formal NOVALIDirectiveBootstrapFile JSON documents here.

These files are operator inputs. Canonical startup authority still begins only after the selected directive is compiled through directive-first bootstrap.
